import "./style.css"

const Banner = () => {
    return (
        <header className="banner">
            <img src="/images/banner.png" alt="Main banner of the page" />
        </header>
    );
}

export default Banner;